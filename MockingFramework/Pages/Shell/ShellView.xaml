<Page
	x:Class="MockingFramework.Shell.ShellView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MockingFramework.Shell"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:cal="using:Caliburn.Micro"
	mc:Ignorable="d">
	
	<NavigationView x:Name="Navigation" IsSettingsVisible="False" MenuItemsSource="{Binding Items}"
					CompactPaneLength="50" OpenPaneLength="200" Header="Mocking Frameworks" IsPaneOpen="False"
					cal:Message.Attach="[Event SelectionChanged] = [Action NavigationChanged($eventArgs)]"
					SelectedItem="{Binding SelectedItem}">
		<NavigationView.MenuItemTemplate>
			<DataTemplate>
				<NavigationViewItem Background="Transparent" Icon="{Binding Icon}" Content="{Binding DisplayName}" Width="200" />
			</DataTemplate>
		</NavigationView.MenuItemTemplate>
		<ContentControl x:Name="ActiveItem" />
	</NavigationView>
</Page>
