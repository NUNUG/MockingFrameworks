<Page
	x:Class="MockingFramework.Pages.Users.UsersView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MockingFramework.Pages.Users"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock Text="Users" Style="{ThemeResource HeaderTextBlockStyle}" Margin="15" />
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="8*" />
			</Grid.ColumnDefinitions>
			<StackPanel>
				<TextBlock Text="Filter Users" Margin="10" />
				<ComboBox x:Name="Filter" HorizontalAlignment="Stretch" Margin="10"></ComboBox>
			</StackPanel>
			<ListBox x:Name="Users" Grid.Column="1">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel>
							<TextBlock Text="{Binding UserId}" />
							<TextBlock Text="{Binding Username}" />
							<TextBlock Text="{Binding Email}" />
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Grid>
	</Grid>
</Page>
